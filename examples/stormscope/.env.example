# Copy to .env.local and fill in your keys.
# AI calls go directly to OpenAI from the browser (demo only).
# In production, route through your own backend proxy.
VITE_OPENAI_API_KEY=sk-...

# Airtable Personal Access Token (pat...) — enables knock-status sync to Airtable.
# Base: Roof Oracle — Lafayette Parish (appuHX1OJ9NITew6i)
# Create one at: https://airtable.com/create/tokens
VITE_AIRTABLE_TOKEN=pat...
